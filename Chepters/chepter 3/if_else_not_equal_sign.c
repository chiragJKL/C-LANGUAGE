#include <stdio.h>
int main()

{
    int num;

    printf("Enter Runs =\n");
    scanf("%d", &num);

    // in c language "!=" is for not equals to

    // again!--> ! "=" is termed as 1 means ON and takes any value rather than 0 see as same

    if (num != 50)
    {
        printf("NOT A HALF CENTURY\n");
    }

    return 0;
}