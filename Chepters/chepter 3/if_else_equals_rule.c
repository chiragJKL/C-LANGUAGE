#include <stdio.h>
int main()

{
    int num;

    printf("Enter Runs =\n");
    scanf("%d", &num);

    // in c language "==" is required to equalise

    // "=" is termed as 1 means ON and takes any value rather than 0 see as same

    if (num == 50)
    {
        printf("HALF CENTURYYYY!!!\n");
    }

    return 0;
}