

//we use gcc compiler. so if we open programme without compiling it. it wont run in powershell
// hence we compile is via downloding extention or
// simply "gcc <file name>" after that compiler will make enter "a then TAB" because it will make exe file name as "a.exe"
//to avoid that "gcc -o" will create exe file with same name as c file.

#include <stdio.h>

int main()
{
    printf("Hello I");
    // sdfhsbfjhj
    // dfjdnf
}
